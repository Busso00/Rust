
const NUMBER:i32 = 3;
fn main() {
    //let x:f32=0.0; //doesn't work
    let x:i32=0;
    if x == 10 {
        println!("x is ten!");
    } else {
        println!("x is not ten!");
    }
    let x1: i32=1;
    println!("Number {}", x1);
    //variables4
    let number = "T-H-R-E-E"; // don't change this line
    println!("Spell a Number : {}", number);
    let number = 3; // don't rename this variable
    println!("Number plus two is : {}", number+ 2);
    println!("{}",NUMBER);

}